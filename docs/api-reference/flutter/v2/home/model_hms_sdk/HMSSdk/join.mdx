# join method

    *[<Null safety>](https://dart.dev/null-safety)*

void join
({required [HMSConfig](../../model_hms_config/HMSConfig-class) hmsConfig})

## Implementation

```dart
void join({
  required HMSConfig hmsConfig,
}) {}
```
