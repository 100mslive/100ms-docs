---
title: Join Room
nav: 5
---

A user can interact with participants of a room only after _joining_ the room. 
When user indicates that they want to _join_ the room, at the moment in code path the app should have following -
1. Room ID 
To join a room - 
1. Create an instance of the 100ms iOS SDK using the `build` class function of `HMSSDK` class
2. 
 create a `HMSConfig` object.

```swift
```
